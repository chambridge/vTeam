# Machine-specific developer config (copy to .env.local — gitignored)
#
# Tailscale IP or hostname of your Linux build box.
# When set, kind clusters run there and kubeconfig is rewritten automatically.
KIND_HOST=100.x.x.x
